const path = '../images/'
const png = '.png'

export const images = {
	landingBG: require(`${path}landing_bg${png}`),
	USFlag: require(`${path}us_flag${png}`),
	errorImg: require(`${path}error_img${png}`),
}
